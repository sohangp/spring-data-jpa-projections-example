<?xml version="1.0" encoding="UTF-8" ?>
<entity-mappings>xmlns="http://xmlns.jcp.org/xml/ns/persistence"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/persistence
    http://xmlns.jcp.org/xml/ns/persistence/persistence_2_1.xsd"
    version="2.1">

    <!-- Application Details Fetch -->
    <named-native-query name="customerEntity.getCustomerDetails">
        <query><![CDATA[
            SELECT
                cust.id as customerId,
                cust.FIRST_NAME as firstName,
                cust.LAST_NAME as lastName,
                cust.CITY as city,
                cust.COUNTRY as country,
                customerOrder.ORDER_NUMBER as orderNumber,
                customerOrder.TOTAL_AMOUNT as totalAmount
            FROM CUSTOMER cust LEFT JOIN CUSTOMER_ORDER customerOrder
                 ON customerOrder.CUSTOMER_ID = cust.ID
         ]]></query>
    </named-native-query>
</entity-mappings>
